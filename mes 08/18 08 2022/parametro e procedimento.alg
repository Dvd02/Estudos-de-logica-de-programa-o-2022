algoritmo "Faz Soma Procedimento"

procedimento soma(A, B : inteiro) : inteiro
var
  z : inteiro
inicio
  z <- a + b
  escreval(z)
fimprocedimento

var
   x, y, w, t : inteiro
inicio
   leia(x)
   leia(y)
   soma(x,y)
   leia(w)
   leia(t)
   soma(w,t)
   soma(8,2)
fimalgoritmo
